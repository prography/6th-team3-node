# Migration `20200602164932`

This migration has been generated by boorim98 at 6/2/2020, 4:49:32 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE `petmily`.`hotel` DROP COLUMN `sat_close_time`,
ADD COLUMN `sat_close_time` varchar(191)   ,
DROP COLUMN `sat_open_time`,
ADD COLUMN `sat_open_time` varchar(191)   ,
DROP COLUMN `sun_close_time`,
ADD COLUMN `sun_close_time` varchar(191)   ,
DROP COLUMN `sun_open_time`,
ADD COLUMN `sun_open_time` varchar(191)   ,
DROP COLUMN `week_close_time`,
ADD COLUMN `week_close_time` varchar(191) NOT NULL  ,
DROP COLUMN `week_open_time`,
ADD COLUMN `week_open_time` varchar(191) NOT NULL  ;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200526000349..20200602164932
--- datamodel.dml
+++ datamodel.dml
@@ -1,6 +1,6 @@
 datasource mysql {
-  url = "***"
+  url      = env("DATABASE_URL")
   provider = "mysql"
 }
 generator client {
@@ -18,14 +18,14 @@
   addressDetail    String?      @map("address_detail")
   zipcode          String?
   latitude         Float
   longitude        Float
-  weekOpenTime     DateTime     @map("week_open_time")
-  weekCloseTime    DateTime     @map("week_close_time")
-  satOpenTime      DateTime?    @map("sat_open_time")
-  satCloseTime     DateTime?    @map("sat_close_time")
-  sunOpenTime      DateTime?    @map("sun_open_time")
-  sunCloseTime     DateTime?    @map("sun_close_time")
+  weekOpenTime     String       @map("week_open_time")
+  weekCloseTime    String       @map("week_close_time")
+  satOpenTime      String?      @map("sat_open_time")
+  satCloseTime     String?      @map("sat_close_time")
+  sunOpenTime      String?      @map("sun_open_time")
+  sunCloseTime     String?      @map("sun_close_time")
   weekPrice        Int          @map("week_price")
   satPrice         Int?         @map("sat_price")
   sunPrice         Int?         @map("sun_price")
   phoneNumber      String       @map("phone_number")
@@ -104,17 +104,17 @@
   @@map("pet")
 }
 enum Gender {
-  MALE @map("수컷")
-  FEMAIL @map("암컷")
+  MALE    @map("수컷")
+  FEMAIL  @map("암컷")
-   @@map("gender")
+  @@map("gender")
 }
 enum Target {
-  PET @map("pet")
-  USER @map("user")
-  HOTEL @map("hotel")
+  PET    @map("pet")
+  USER   @map("user")
+  HOTEL  @map("hotel")
-   @@map("target")
-}
+  @@map("target")
+}
```


